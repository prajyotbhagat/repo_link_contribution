# Global options — swap to ZeroSSL to avoid Let's Encrypt rate limits on shared
# sslip.io subdomains. Replace the email with yours.
{
	acme_ca https://acme.zerossl.com/v2/DV90
	email   [prajyotbhagat1989@gmail.com]   # ← replace with your real email
}

13-48-26-213.sslip.io {
	# Reverse-proxy all traffic to the Django/Gunicorn container
	reverse_proxy web:8000

	# Serve pre-collected static files directly from Caddy for performance
	handle_path /static/* {
		root * /app/staticfiles
		file_server
	}

	# Standard security headers
	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
		X-Content-Type-Options    "nosniff"
		X-Frame-Options           "DENY"
		Referrer-Policy           "strict-origin-when-cross-origin"
	}

	# Access log
	log {
		output stdout
		format json
	}
}
